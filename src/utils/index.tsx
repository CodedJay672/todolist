export enum Priority {
  LOW = 'Low',
  MEDIUM = 'Medium',
  HIGH = 'High',
}

export type Person = {
  name: string;
  email: string;
  phone: string;
}

export enum Labels {
  HTML = 'html',
  CSS = 'Css',
  NODEJS = 'Nodejs',
  JQUERY = 'Jquery',
}